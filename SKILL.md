---
name: semantic-sketch-collage
description: Use when a user wants supplied photographs restyled as a bold hand-drawn editorial collage, experimental poster, photo-sketch composition, zine artwork, or semantically extended portrait, object, interior, travel, or mixed-media image.
---

# 语义延展式手绘拼贴

将真实照片转化为大胆、随性、具有实验出版物气质的手绘拼贴。纸面是第一画布层，照片是嵌入纸面的现实片段；线稿从照片的真实结构继续生长，文字参与构图并完成叙事。

**核心不变量：每一个新增线稿和文字都必须同时具备语义来源、空间锚点和构图功能。** 仅仅让画面“更丰富”不构成添加理由。

**运行时输入不变量：用户提供的照片是图像工具唯一允许接收的图片。用户上传一张图片，图像工具就只能接收这一张；只有用户为当前作品明确提供多张图片时才可使用多图编辑。Bundled reference 不得作为额外图片上传。**

**多张待测照片默认逐张独立处理：** 用户一次上传多张但没有要求合成为同一作品时，为每张照片建立独立的事实、叙事链和图像调用；不让其中一张照片充当另一张的风格或事实参考。

## 适用边界

- 适用于人物、物件、空间、旅行、生活纪实、活动海报和多图叙事。
- 不适用于仅要求普通素描滤镜、完整写实重绘、照片修复或无来源贴纸装饰的请求。
- 用户只要求分析或提示词时，不调用图像生成；用户要求制作图片时，使用 `imagegen` 完成最终位图，不停在方案描述。

## 必守规则

1. 先读照片，后看风格参考。用户照片始终是人物、物件、场景、透视、光线和身份信息的事实源。
2. 每张作品只建立一条主要叙事链，使用一至三个互相关联的语义元素族。
3. 新增元素通过 `来源 -> 锚点 -> 视觉作用` 三项检查后才能进入画面。
4. 保持主体可辨识。人物面部默认受保护；只有明确的心理肖像意图才允许线稿重构五官。
5. 大胆来自裁切、尺度、方向、跨界和疏密反差，不来自随机增加元素。
6. 用户文字原样保留。没有用户文字时，只使用可从照片或用户背景可靠支持的短语；证据不足就不加文字。
7. Bundled reference 只供内部理解和提炼转换语法；把结论写成文本提示，不把参考文件附加到图像工具或外部服务。
8. 默认不是在全幅照片上覆盖线稿。先依据原图配色选择浅色纸面，再把原照片裁成局部照片块、窗口或抠图主体；线稿必须越过照片边缘，在纸面上补全、替代或延展被裁掉的空间。淡米白减少黄感，保留已认可的雾蓝灰作为候选，具体选择见 [visual-language.md](references/visual-language.md)。
9. 跨界必须保持同一对象的尺度、透视、方向和遮挡关系。线稿先在照片内部接住真实轮廓，再越过边缘继续；不能在照片旁边重新画一套完整主体。
10. 照片与纸面共享印刷颗粒、对比度和来自照片的强调色。清晰照片边缘可以存在，但必须被结构线、文字或描边穿越，不能形成悬浮卡片感。
11. 照片存在清晰人物或物件轮廓、且版面需要承载较多文字时，可把同一轮廓做成偏移的纸面色负形剪纸；它只承担遮挡、连接和排字，不成为第二个事实主体。
12. 用户确认某一版的照片—线稿关系后，后续只要求改文字、外框或边缘时，进入**锁定底图局部改版**；不重新生成已经成立的主体、跨界结构轴或交互接缝。
13. Vox 抠图感通过稍宽的断续纸边和取自原图的局部错位叠色增强；先划定手绘转化区域，再在非交互侧设计纸边和色层，不能沿整圈统一衬边。边缘从属于原色照片与跨界线稿，色块有清楚的局部版面作用但不扩张成大面积纯色底板。
14. 在纸面色调、手绘丰富度、断续外框、源色色块和文字层级之间建立完整编辑版式；克制不等于删成白底照片加两个标题。中日主标题遵从用户指定语言和字形，英文辅助不得因粗黑或体量抢走主标题位置，详见视觉与文字参考。

## 工作流

### 生图执行分工

用户已指定：后续生图由执行型子 agent 完成，主 agent 负责审查验收、反馈修正和与用户确认交付。主 agent 在当前任务中委派，不另建用户任务；向子 agent 提供本 skill 路径、当前照片/已确认底图、准确文字、用户最新偏好、输入边界、已有授权与输出路径。子 agent 自行执行而不递归委派，负责读图、编译提示、图像调用及必要的局部合成，返回成图路径与检查结果；主 agent 必须亲自查看正常尺寸和缩略图、核对文字与跨界关系，局部改版核对保护区像素。发现具体问题交回同一子 agent 定向修改，审核通过后直接展示图片，最终认可由用户决定。仅总结、规则更新或无需生图的讨论不启动生成子 agent。

### 制作步骤

本项目已配置并成功使用 CLI 调用 ZenMux `gpt-image-2`。后续由生图子 agent 直接复用已配置的 CLI、运行环境和安全凭证存储执行，不要求用户打开终端、粘贴命令、手动触发或重复配置密钥。已有授权在原范围内持续有效；实际故障先自行诊断，仅在确实需要用户操作或新增授权时说明具体阻碍。不得把读取凭证的结果写入日志、提示词或交付内容。

1. 读取 [semantic-analysis.md](references/semantic-analysis.md)，建立 `Semantic Visual Card`，区分用户事实、可见事实和不可验证推测。
2. 读取 [interaction-modes.md](references/interaction-modes.md)，选择人物、动作、物件、空间或混合模式，并建立交互图。
3. 读取 [visual-language.md](references/visual-language.md)，确定照片处理、线条、构图、色彩和内部视觉线索。
4. 画面含文字时读取 [lettering-system.md](references/lettering-system.md)；中文与日文标题分别采用其中固定参考字形，英文保持配套层级；无文字时跳过。
5. 读取 [prompt-and-quality.md](references/prompt-and-quality.md)，编译生成提示、调用图像工具并执行质量检查。
6. 只有用户要求在已确认版本上做定向迭代时，按 [prompt-and-quality.md](references/prompt-and-quality.md) 的“锁定底图局部改版”执行遮罩编辑与像素回贴。

## 默认决策

- 构图强度：大胆，但保持一个清晰主体。
- 画布：默认 3:4 或 4:5 竖版，背景根据原图自行选择相协调的浅色纸面，包括淡米白、已认可的雾蓝灰和中性浅灰；用户指定比例或颜色时遵从用户。
- 照片真实性：高，默认保留原图原色及光线关系；环境/物件照片块通常覆盖纸面约 20%–45%，人物抠图可放大到约 35%–65%；允许大胆裁切、抠图和局部拼接，但不可默认全幅铺底或黑白化。
- 线稿：黑白自适应、轻微抖动、重复描线、局部速写乱线。
- 文字：`grounded`；默认使用一组主标题和一至三组有依据的辅助短语，并在展示字、叙事字和微型注释字中选择两至三种字级，以不规则手写、下划线或圈注参与版面；证据不足时自动降级为 `none`。
- 作品资产参考：最多读取一张主要成品，可选一张只补充单一行为的成品；仅提炼为文字，不作为生成输入。
- 修正：发现明确失败时进行一次定向修正，不使用相同提示盲目重试。

## 输出

默认返回完成图片和一段简短中文创作说明，说明主要叙事链与照片—线稿交互。只有用户明确要求时才附完整提示词、Semantic Visual Card 或详细构图参数。
